name: FlappyBird
args:
  random_seed: null

  # Get screen in grayscale NumPy array with shape (height, width) or (color, height, width)
  grayscale: True

  # Resize the observation
  width: 64
  height: 96

  fastforward: True
  play_sound: False
